# plot_all_rss.py

## Purpose

Plot each RSS server separately as multiplots, with dual axis:

### Module notes

Plot each RSS server separately as multiplots, with dual axis:

Left axis  : Primary -> ACK gap (pages)
Right axis : storage await/service time from OSMON (optional)

Input format matches your existing rss_repl_plot_refactored.py parser.

Example:
  python3 plot_all_rss.py replication_sat20.log new_data/ld620/osmon_sum_1.log       --start "2026-06-20 15:00" --end "2026-06-20 18:30" --smooth 10

If you do not want OSMON overlay:
  python3 plot_all_rss.py replication_sat20.log --start "2026-06-20 15:00" --end "2026-06-20 18:30"

### CLI description

Plot Primary -> ACK for each RSS server as multiplots with optional OSMON dual axis.

## Location

`rss/plot_all_rss.py`

## Usage

```bash
python rss/plot_all_rss.py \
    VALUE \
    VALUE
```

## Command line options

| Option | Required | Default | Choices | Description |
|---|---:|---|---|---|
| `rss_log` | Yes | `` | `` |  |
| `osmon_file` | Yes | `` | `` | Optional OSMON summary file (nargs: `?`) |
| `--start` | No | `` | `` |  |
| `--end` | No | `` | `` |  |
| `--servers` | No | `` | `` | (nargs: `*`) |
| `--log-size` | No | `LOG_SIZE` | `` | (type: `int`) |
| `--smooth` | No | `1` | `` | (type: `int`) |
| `--log-scale` | No | `` | `` | (action: `store_true`) |
| `--per-page` | No | `6` | `` | Server panels per PDF page (type: `int`) |
| `--top-n` | No | `` | `` | Only plot top N servers by max Primary -> ACK gap (type: `int`) |
| `--out` | No | `all_rss_server_panels_primary_ack.pdf` | `` |  |
| `--summary-csv` | No | `all_rss_server_panels_summary.csv` | `` |  |
| `--timeseries-csv` | No | `all_rss_server_panels_timeseries.csv` | `` |  |

## Detected inputs

| Path/expression | Method | Type | Code location |
|---|---|---|---|
| `filename` | open | file/path expression | line 80 |

## Detected outputs

| Path/expression | Method | Type | Code location |
|---|---|---|---|
| `out_pdf` | PdfPages | PDF report | line 301 |
| `fig` | savefig | image/plot output | line 320 |
| `args.summary_csv` | to_csv | CSV file | line 406 |
| `args.timeseries_csv` | to_csv | CSV file | line 407 |

## Dependencies

- `matplotlib`
- `numpy`
- `pandas`

## Functions

### `page_distance(new_log, new_page, old_log, old_page, log_size=LOG_SIZE)`

No function docstring detected.

### `parse_rss_line(line)`

No function docstring detected.

### `load_rss_all(filename, start=None, end=None, servers=None, log_size=LOG_SIZE)`

No function docstring detected.

### `load_osmon(filename, start=None, end=None)`

No function docstring detected.

### `merge_osmon(rss, osmon)`

No function docstring detected.

### `make_summary(df)`

No function docstring detected.

### `quarter_windows(df)`

No function docstring detected.

### `apply_y_limits(ax, series, log_scale=False)`

No function docstring detected.

### `plot_server_panel(ax, g, osmon, server, smooth=1, log_scale=False, show_storage=True)`

Per-server panel.

Left Y axis  : Primary -> ACK gap (large backlog, pages)
Right Y axis : ACK -> Apply gap (bounded pipeline lag, pages)

Optional OSMON storage is shown as a faint third axis offset to the right so
it does not squash or distort the ACK -> Apply scale.

### `plot_multipage_panels(df, osmon, summary, out_pdf, smooth=1, log_scale=False, per_page=6, top_n=None)`

No function docstring detected.

### `parse_args()`

No function docstring detected.

### `main()`

No function docstring detected.

## Notes

- This page was generated by `build_repo_docs.py` using static AST analysis.
- Review examples and detected input/output paths before publishing if the script builds paths dynamically.
