# summarise_disk.py

## Purpose

summarise_diskstats.py

### Module notes

summarise_diskstats.py

Python 2.6 compatible text summariser for iostats_v26_mpath_paths.py CSV output.

Adds an explicit layer judgement per multipath:

  LV await >> mpath await ~= path await
      => difference appears above mpath, i.e. LV/device-mapper/LVM -> mpath

  LV await ~= mpath await ~= path await
      => behaviour tracks through mpath/paths, i.e. likely at/below path/storage side

  mpath await ~= path await
      => mpath and underlying sd paths are consistent

  mpath await >> path await
      => possible host multipath/device-mapper aggregation issue

Use:
  python summarise_diskstats.py -i disk_stats.csv --multipath-file /tmp/multipath_ll.txt --mpath dm-8
  python summarise_diskstats.py -i disk_stats.csv --multipath-file /tmp/multipath_ll.txt -o disk_summary.txt

## Location

`io/summarise_disk.py`

## Usage

```bash
python io/summarise_disk.py
```

## Command line options

No argparse options detected.

## Detected inputs

| Path/expression | Method | Type | Code location |
|---|---|---|---|
| `path` | open | file/path expression | line 106 |
| `filename` | open | file/path expression | line 182 |

## Detected outputs

| Path/expression | Method | Type | Code location |
|---|---|---|---|
| `options.output_file` | open | output/text dump | line 731 |

## Dependencies

- `__future__`
- `optparse`

## Functions

### `safe_float(value, default=0.0)`

No function docstring detected.

### `percentile(values, pct)`

No function docstring detected.

### `avg(values)`

No function docstring detected.

### `max_value(values)`

No function docstring detected.

### `first_non_blank(values)`

No function docstring detected.

### `emit(lines, text=)`

No function docstring detected.

### `read_text_file(path)`

No function docstring detected.

### `parse_multipath_text(text)`

No function docstring detected.

### `save_current()`

No function docstring detected.

### `load_csv(filename)`

No function docstring detected.

### `enrich_rows_with_multipath_file(rows, multipath_file)`

No function docstring detected.

### `group_by_device(rows)`

No function docstring detected.

### `metric_values(rows, metric)`

No function docstring detected.

### `device_summary(rows)`

No function docstring detected.

### `build_device_summaries(grouped)`

No function docstring detected.

### `rank_devices(summaries, metric, stat, device_filter=None)`

No function docstring detected.

### `is_mpath_device(dev, s)`

No function docstring detected.

### `is_system_device(dev, s, options=None)`

Return True for OS/system devices that should be ignored in DB/storage evidence views.

### `is_lvm_or_dm_device(dev, s)`

No function docstring detected.

### `make_lvm_filter(options)`

No function docstring detected.

### `related_paths_for_mpath(parent_dm, summaries)`

No function docstring detected.

### `child_lvs_for_mpath(parent_dm, summaries)`

No function docstring detected.

### `ratio(a, b)`

No function docstring detected.

### `classify_layers(parent, path_summaries, lv_summaries, threshold_ratio, threshold_abs_ms)`

Classify where the behaviour appears to diverge.

Uses p95 await as the main signal because that is the metric Andrew is using
to reason about LV vs mpath vs path behaviour.

### `format_summary_line(dev, s)`

No function docstring detected.

### `print_rank_section(lines, title, ranked, limit)`

No function docstring detected.

### `top_lvs_near_mpath(parent_dm, summaries, options)`

Return (ranked_lvs, is_direct_mapping).

Direct mapping is only available if LV rows have mpath_parent_dm populated.
If not, return global top LVs as context only and mark as not direct.

### `print_layer_rule(lines)`

No function docstring detected.

### `print_mpath_correlation(lines, parent_dm, summaries, options)`

No function docstring detected.

### `print_all_mpath_correlations(lines, summaries, options)`

No function docstring detected.

### `build_report(rows, summaries, options)`

No function docstring detected.

### `parse_options()`

No function docstring detected.

### `main()`

No function docstring detected.

## Notes

- This page was generated by `build_repo_docs.py` using static AST analysis.
- Review examples and detected input/output paths before publishing if the script builds paths dynamically.
