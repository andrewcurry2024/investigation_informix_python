# iof.py

## Purpose

Compare Informix onstat -g iof AIO global files between good and bad RSS periods.

### CLI description

Compare Informix onstat -g iof AIO global files between good and bad RSS periods.

## Location

`io/iof.py`

## Usage

```bash
python io/iof.py \
    VALUE \
    VALUE
```

## Command line options

| Option | Required | Default | Choices | Description |
|---|---:|---|---|---|
| `bad_iof` | Yes | `` | `` | Path to bad RSS iof.out |
| `good_iof` | Yes | `` | `` | Path to good RSS iof.out |
| `--mode` | No | `last` | `['last', 'sum']` | How to handle duplicate pathnames. Default: last |
| `--prefix` | No | `iof` | `` | Output file prefix. Default: iof |

## Detected inputs

| Path/expression | Method | Type | Code location |
|---|---|---|---|
| `path` | open | file/path expression | line 402 |

## Detected outputs

| Path/expression | Method | Type | Code location |
|---|---|---|---|
| `output` | savefig | image/plot output | line 49 |
| `raw_csv` | to_csv | CSV file | line 751 |
| `reduced_csv` | to_csv | CSV file | line 752 |
| `comp_csv` | to_csv | CSV file | line 753 |

## Dependencies

- `matplotlib`
- `pandas`

## Functions

### `plot_top_delta_barh(comp, metric, title, xlabel, output, top_n=20)`

Horizontal top-N delta chart.
Best for readable 'bad minus good' evidence.

### `plot_good_bad_for_pattern(comp, metric, pattern, title, ylabel, output)`

Compare good vs bad for pathnames matching a pattern, e.g. tmpdbs or llogdbs.

### `plot_temp_kaio_explosion(comp, output)`

Specific graph for tmpdbs01-06 KAIO total seconds.
This supports the 'temp dbspaces exploded' narrative.

### `plot_temp_delta_pct(comp, output)`

Specific temp percentage increase chart.
This is good for making the >12,000% point visually obvious.

### `plot_service_time_top_read(comp, output, top_n=20)`

Good vs bad read service time for the top degraded chunks.

### `plot_service_time_top_write(comp, output, top_n=20)`

Good vs bad write service time for the top degraded chunks.

### `plot_log_space_kaio(comp, output)`

Specific graph for logical log dbspaces.
Supports the log-driven RSS apply/recovery argument.

### `parse_iof(path, label)`

Parse Informix onstat -g iof / AIO global files output.

Expected format:

AIO global files:
gfd pathname         bytes read     page reads  bytes write    page writes io/s
3   rootdbs_01       242712576      118512      1088231424     531363      751.9
        op type     count          avg. time
        seeks       0              N/A
        reads       0              N/A
        writes      0              N/A
        kaio_reads  47320          0.0014
        kaio_writes 246025         0.0013

### `add_derived_metrics(df)`

No function docstring detected.

### `reduce_duplicates(df, mode)`

Reduce duplicate pathnames.

mode=last:
    Use the last occurrence for each pathname.
    Best when iof.out contains repeated onstat snapshots.

mode=sum:
    Sum counters/volumes and recalculate weighted averages.
    Useful if duplicates are genuinely separate rows you want combined.

### `build_comparison(good, bad)`

No function docstring detected.

### `plot_delta(comp, metric, title, ylabel, output, top_n=20)`

No function docstring detected.

### `print_top_table(comp, metric, n=20)`

No function docstring detected.

### `main()`

No function docstring detected.

## Notes

- This page was generated by `build_repo_docs.py` using static AST analysis.
- Review examples and detected input/output paths before publishing if the script builds paths dynamically.
